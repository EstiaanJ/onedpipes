[package]
name = "onedpipes"
version = "0.1.0"
edition = "2024"

[dependencies]
eframe = "0.32"
plotters = { version = "0.3", default-features = false, features = [
    "bitmap_backend",
    "line_series",
] }
